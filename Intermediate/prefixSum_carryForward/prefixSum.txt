--> sum of elements with thin the range 
// tc - O(N)
// sc - O(1)

int[] arr = {1, 2, 3, 4, 5}

// 1. create a prefix array
int[] prefixArr = {1, 3, 6, 10, 15}

// 2. query execution
if(start == 0) sum = end
query range calculation for sum => end - (start-1)

---------------

--> sum of even indexed elements
// tc - O(N)
// sc - O(1)

int[] arr = {1, 2, 3, 4, 5}

// 1. create a even prefix array
int[] prefixArr = {1, 1, 4, 4, 9}

// 2. query execution
if(start == 0) sum = end
query range calculation for sum => end - (start-1)
